spring.application.name=scm
server.port=8083

spring.data.mongodb.uri=mongodb+srv://<contact_user>:Ritik%40123@cluster0.lwi4sfe.mongodb.net/?appName=Cluster0

# MongoDB configuration
spring.data.mongodb.database=scm_db

# Commented out SQL configuration as we're switching to MongoDB
# spring.datasource.url=jdbc:h2:mem:scm_db;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# Commented out JPA configuration as we're switching to MongoDB
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true


#spring.secqurity configuration

#oauth2 client configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.client-name=Google

spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET}
spring.security.oauth2.client.registration.github.scope=user:email
spring.security.oauth2.client.registration.github.client-name=GitHub

# Allow circular references
spring.main.allow-circular-references=true

# File upload configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
